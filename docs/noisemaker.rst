Noisemaker CLI
==============

Noisemaker includes the CLI utility ``noisemaker``.

This script is still new, and documentation is forthcoming. In the meantime, run ``noisemaker --help`` for a complete list of options.

.. literalinclude:: noisemaker-help.txt
   :language: bash
